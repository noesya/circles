<% content_for :title, 'Utilisateurices' %>

<table class="table">
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user, user %></td>
        <td><%= user.people.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>