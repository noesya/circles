<% content_for :title, 'Utilisateurices' %>

<table class="table">
  <thead>
    <tr>
      <th>Utilisateurice</th>
      <th>Cercle</th>
      <th>Personnes importées</th>
      <th>Personnes avec des interactions</th>
      <th>Interactions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user, user %></td>
        <td><%= user.people_in_circle.count %></td>
        <td><%= user.people_imported.count %></td>
        <td><%= user.people_with_interactions.count %></td>
        <td><%= user.interactions.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>