<% content_for :title, 'Personnes' %>

<p><%= @people.total_count %></p>

<div class="row">
  <% @people.each do |person| %>
    <div class="position-relative col-xl-2">

      <%= link_to person.to_s, person, class: 'stretched-link' %>
    </div>
  <% end %>
</div>

<%= paginate @people %>